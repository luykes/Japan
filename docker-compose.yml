services:
  app:
    build: .
    container_name: japan-travel
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - DATA_DIR=/data
    volumes:
      - checklist_data:/data

volumes:
  checklist_data:
